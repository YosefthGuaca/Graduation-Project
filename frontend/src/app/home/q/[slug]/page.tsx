import React from 'react';
import Sidebar from '@/app/components/Sidebar';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Sidebar />
      This is Editing Page for user
    </div>
  );
};

export default page;
